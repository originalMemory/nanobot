# lover 基于新上游重建：已确认范围与待讨论方案

更新：2026-09-18

状态：F01 Electron、F02 统一收件箱、F03 桌面基础体验已完成；F04 聊天展示与设置已确认复用上游，待实际体验。F05 外观已完成；后续为 F06 工作区与笔记浏览。

## 1. 总体方向

从指定上游版本在当前项目建立并切换新分支，以最新 WebUI 作为 Electron 渲染层基础，按需要补回个人功能。不要逐个重放 lover 的历史提交，不再维护一套完整复制后长期分叉的聊天 UI。

产品仍是伴侣 AI：保留统一收件箱、跨渠道的连续对话，不以多话题、多会话工作台为主要交互。对话运行、子代理、cron、心跳先使用最新上游，体验后再决定需要调整的部分。空闲压缩关闭；用户进一步倾向沿用 lover 的压力触发、部分压缩与原文移出活跃文件的行为，具体与上游集成边界见 5.5。

原始对话不能因自动压缩或存储裁剪而彻底销毁，必须有可追溯、可读取的持久化记录。摘要、Dream 记忆、主题卡均不能代替原文保存。

准备进展：本地 main 已快进到上游 `4420ad58`，已从 `v0.3.5` 建立 `lover-next` 分支，并在当前项目目录切换到该分支；额外 worktree 已移除。已新增 Electron 基础壳并完成第一轮 review 修复；尚未迁移运行数据或删除旧功能。F15、F16、F19 的“不迁移”已记录；不在本轮清理旧 lover 或未跟踪的规格文件。

### 核对基线

| 项目 | 版本与证据范围 |
| --- | --- |
| lover | `54ca43deaea979261c7815707da10db642cde308`；当前 HEAD 未变 |
| 最新上游 | 本轮重新 fetch 默认分支，固定到 `4420ad58e10c5a86f055eb498a7e4f0a59eaed6c`，2026-09-18 00:25 +08:00 |
| 上游只读分析目录 | `/tmp/nanobot-upstream-Si5oSq`，已更新到上述提交；临时目录可能被清理 |
| 方法 | 当前代码与调用路径核对，历史规格仅作背景；未运行应用和测试，未读取线上私人数据库或配置 |

**纠正上一版：lover 当前已经使用按月 JSONL 原文归档，不是 SQLite。** `SessionHistoryStore` 类名以及部分 docstring、旧 OpenSpec 仍保留 SQLite 描述，但当前写入与搜索实现都是 JSONL/grep。旧文档不能作为当前存储格式的证据。

## 2. 第一轮功能决策

| ID | 能力 | 用户决定 | 实施边界 |
| --- | --- | --- | --- |
| F01 | Electron | 保留，整体复用最新 WebUI | 先适配桌面宿主，再小范围调整体验；不整套搬旧 renderer |
| F02 | 统一收件箱 | 保留；伴侣 AI 不需要多会话体验 | 首页与主要操作围绕统一聊天；上游内部会话结构不必全部删除 |
| F03 | 桌面基础体验 | 保留 | 托盘、快捷键、截图附件、通知、启动偏好 |
| F04 | 聊天展示与设置 | 先复用最新 WebUI | 思考、工具、diff、用量与设置先体验，再补缺漏和外观问题 |
| F05 | 外观 | 保留 | 主题、头像名称、透明与壁纸等已选定体验；沿用最新 UI 底座实现 |
| F06 | 工作区与笔记浏览 | 保留 | 文件预览、日记/笔记库、今日日记入口 |
| F07 | 独立日记检索工具 | 倾向不迁移；用户提出是否已无必要 | 建议不暴露专用工具，保留 Active Memory 依赖的搜索函数；详见第 4 节 |
| F08 | 自动记忆召回、主题卡 | 按建议保留 | 迁移当前有效逻辑，适配上游扩展接口，不复活旧演进方案 |
| F09 | Dream 个人化记忆 | 按建议保留 | 使用上游基础实现，保留人格、经历、时间与来源语义 |
| F10 | 裁剪与历史存档 | 不需要 SQLite；要求保住原始记录 | 当前已是月度 JSONL；新版本优先上游原文保留机制，补齐销毁边界 |
| F11 | 定时任务 | 先用最新上游 | 暂不移植旧 cron 隔离/通知机制；试用时观察是否产生聊天噪声 |
| F12 | 心跳与主动陪伴 | 心跳用上游；桌面感知要有，方案重议 | 截图、失焦、锁屏、静默时段如何组合尚未确定，不直接搬旧触发机制 |
| F13 | TTS | 只保留 MiniMax | 保留自动朗读、重播、暂停系统媒体；不迁移 IndexTTS、Qwen 等其他后端 |
| F14 | 数字伴侣 | 只保留本地状态视频 | 保留相关本地状态/场景视频体验；不接实时口型服务 |
| F15 | THA 桌宠 | 不迁移 | 不迁移其服务、设置入口和专属资源链路 |
| F16 | PSB/E-mote 桌宠 | 不迁移 | 不迁移标签协议、专属渲染、窗口与后端资源链路 |
| F17 | 日语教学与 Anki | 放入 workspace 技能 | 不作为内置技能；参考现有独立 bundle 部署，保留教学数据 |
| F18 | NAS/Unraid 部署 | 按建议保留必要部分 | 核对当前挂载、权限、代理、路径，不重放历史环境补丁 |
| F19 | 伴侣天气草案 | 本次不加入 | 从迁移范围移除；旧未跟踪规格暂不动 |

补充决定：对话和子代理功能先使用最新上游，实际体验后再改。旧版的同步等待、并发、错误恢复等补丁不另外移植。

## 3. Electron 复用最新 WebUI：可行，但需要宿主适配

上游 `webui/src/lib/runtime.ts` 已定义 `RuntimeHost` 与 `window.nanobotHost`，支持原生运行环境、目录选择、重启引擎、日志/诊断和 WebSocket bridge。这提供了复用基础；不是把网页塞进窗口后所有旧桌面功能就自然存在。

建议路线：

1. Electron 继续负责窗口、托盘、快捷键、通知、截图、系统媒体控制和本地视频资源等桌面能力。
2. 以同一份上游 WebUI 源码/构建为主要 renderer，复用聊天、Markdown、工具、模型/设置页面和网络协议。
3. 通过现有原生宿主接口连接 Electron；上游没有的必要能力再增加小范围桥接。
4. 在桌面入口适配统一收件箱，减少不需要的多话题/分屏操作；它不是只隐藏侧栏，还要核对历史读取、发送目标和跨渠道事件。
5. 最后补外观、笔记库、MiniMax 播放和本地伴侣视频，不预先复刻旧 UI。

优先让共用组件保持接近上游，桌面特有布局和能力留在 Electron 宿主适配中。若确需修改根 `webui/`，应限定为本次明确要求的 UI 复用和必要接入，不顺带改造浏览器产品其他流程。

需要在实施中核对：远程 NAS gateway 的地址与鉴权、桌面宿主本地文件路径、附件 staging、CSP/原生桥接、重新连接与音频重播。当前仅确认接口基础，没有做 Electron 集成验证。

### 已确认：统一收件箱接口与身份方案

- Electron 使用一个稳定的 `chat_id` 作为发送、订阅、停止与恢复的通信身份，不因每次启动生成新聊天。
- 后端模型会话使用 `unified:default`；它与前端 `chat_id` 不等价。不能把同名 ID 直接交给 WebUI 就假设会自然映射正确。
- 优先扩展现有会话解析，复用 `/api/sessions/{key}/webui-thread` 的响应格式、分页和历史构建，不复制一套 inbox 历史实现。
- 如确需 inbox 专用入口，只增加薄的转接层。跨渠道实时转发、上下文统计与任务控制都要与统一会话映射一致。

## 4. F07：可以省去专用工具，不能删掉 Active Memory 的搜索依赖

用户已知道并会向 AI 提供日记库位置，可以先让模型使用普通文件/搜索工具做主动查阅，不再专门迁移一个日记搜索工具入口。

但 Active Memory 不是日记库的完整替代品：自动召回只挑选相关片段，可能漏掉明确查询所需的细节。当前 `active_memory.py` 还直接从 `diary_search.py` 导入：

- `canonical_diary_files`：冲突副本与重复来源处理；
- `contains_diary_term`、`diary_body`、`diary_excerpt`：命中、正文与片段提取；
- `search_diary_files`：原始日记搜索。

建议迁移时保留这些真正用到的函数作为 Active Memory 内部实现，不再注册 `DiarySearchTool`。不要为了移除模型工具而删除整个文件，也不需要提前造新的检索框架。

验收：自动召回能接上用户经历；用户明确要求查某日日记、某件往事或原文时，普通工具能访问到正式来源。日记库路径和读取权限应进入实际运行配置/工作区说明，仅“知道路径”并不代表沙箱允许访问。

该方案是针对用户疑问的建议，尚未实施。

## 5. 记忆与历史：本地、上游实际如何保存

### 5.1 当前 lover：确实已按月归档 JSONL

当前 `nanobot/session/history_store.py`：

- 写入 `<sessions_dir>/archive/YYYY-MM.jsonl`；当前 SessionManager 的 sessions_dir 是 `<workspace>/sessions`。
- 月份取每条消息自身的 `timestamp[:7]`，没有时间戳时写 `unknown.jsonl`。
- 每行序列化原消息 dict，不是仅保存 LLM 摘要；归档搜索在 `session_search.py` 中使用 grep，不依赖 SQLite。
- `session_key` 和 `reason` 虽然传入写入方法，但目前没有额外包进归档行；不能据此假设归档天然带有完整会话归属信息。

当前自动/手动上下文整合与空闲压缩路径会把裁掉的原始消息送入该归档。除此以外，`memory/history.jsonl` 保存整合摘要/降级材料，供 Dream 使用，是另一种文件，不能混为原始对话归档。

**已发现的保真缺口（只记录，不在本轮修复）：**

- 归档追加失败被捕获并只记 warning，调用方仍可能保存裁剪后的会话；因此不能承诺“当前任何裁剪都不会丢原文”。
- 文件消息数上限路径调用 `enforce_file_cap(on_archive=memory.raw_archive)`，没有走同一个月度原文归档入口；其降级材料也不能等同完整原文备份。

这些是新方案需要避免的问题，不意味着本轮已确认线上发生过数据丢失。

### 5.2 最新上游：正常压缩只改变模型可见窗口，原文仍在会话文件

已核对 `4420ad5` 的 `Session.commit_summary_checkpoint()`、`get_history()`、`Consolidator.compact_idle_session()`、Runner 保存和 JSONL 保存路径：

- 提交累计摘要、更新 `last_archived` 边界，并插入内部继续标记。
- 后续模型请求使用摘要和边界之后的内容，不再重放全部旧消息。
- 原始 `session.messages` 仍保留；保存时逐条写入会话 JSONL。
- 默认会话位置是配置目录的 `sessions/<workspace-id>/`，也支持显式存储根目录。
- 没有发现旧版 `FILE_MAX_MESSAGES` / `enforce_file_cap` 自动裁剪路径。该结论限于核对的提交，不能推广为所有未来版本都永久保留。

所以正常压缩后的“归档位置”仍是原会话 JSONL，而不是另存的月度 archive。**逻辑上不再送进模型，不等于物理删除。**

### 5.3 上游何时触发

| 触发 | 条件/默认值 | 原始对话去向 |
| --- | --- | --- |
| 运行中上下文压力 | 请求估算/对应 provider 用量达到有效输入预算；依模型上下文窗口与预留计算，不是固定消息条数 | 累计摘要检查点替代模型重放，原始会话消息保留 |
| Provider 原生压缩 | 支持该能力的 provider 在其压缩条件满足时执行，上游同步本地摘要检查点 | 本地已持久化会话原文保留；私有 provider state 与公开原文不是一回事 |
| 空闲自动压缩 | 默认空闲 15 分钟；扫描默认最小间隔 60 秒，跳过有运行任务的会话，需要有未归档消息；TTL 为 0 可禁用 | 更新摘要和边界，保留原始会话消息；不是精确到 15 分钟必然执行 |
| 手动 `/compact` | 用户触发，调用同一摘要检查点路径 | 保留原始会话消息 |
| Dream | 默认启用、每 2 小时，通过系统 cron 调度；支持配置调整 | 消费 `memory/history.jsonl` 的未处理材料，更新长期记忆；不等于裁剪原始会话 |

Dream 输入日志另有清理：`compact_history()` 默认上限 1000 条，超过时可移除较旧的已处理记录，同时保护未处理记录。这是记忆加工日志的保留策略，不是会话原文归档策略。若希望连每一版中间摘要也永久保存，需要另行确认；当前明确要求是对话原文不能因裁剪销毁。

### 5.4 “原文不能销毁”还需要覆盖重置/删除路径

最新上游 `/new` 先 `session.clear()` 并保存，再对尚未归档的快照异步生成记忆摘要。它没有先创建完整原文副本。删除会话接口也会删除会话文件。

因此，不能把“普通压缩保留原文”扩大成“任何操作都不会丢原文”。建议新版本遵守下面的最低契约：

1. 自动压缩优先直接沿用上游，不为每次压缩重复存一套月度副本，不引入数据库。
2. 任何会物理移除原始记录的重置、清理或迁移，先完成可验证的原文归档；归档失败则保留原文，不继续破坏性步骤。
3. 删除的含义要明确：普通清理先进入可恢复归档；只有用户明确要求彻底删除时才另行处理。具体交互留到实施方案确认。
4. 旧 lover 的 `sessions/archive/*.jsonl` 原样保护，保持可搜索；不能只迁移活跃会话就遗漏过去裁掉的消息。
5. 摘要、Dream 文件和主题卡均不作为原始会话的替代备份。附件路径与资源也需要可用，JSONL 中有引用不代表附件实体已归档。

待讨论：未来原文是否必须另存按月 JSONL？当前需求用“上游会话文件保留原文 + 物理移除前归档”即可满足主要目标；若必须统一按月查阅，再决定组织形式，不先维护重复存储系统。

### 5.5 第二轮倾向：压力触发、部分压缩、原文归档后移出

用户提出保留 lover 的使用效果：尽量保留近期原始对话，达到上下文容量压力后再压缩，并把已压缩原文移出活跃会话文件。该方向区别于完全沿用上游的全历史摘要检查点；不等于整体搬回旧 Consolidator。

明确采用配置关闭空闲压缩：`agents.defaults.idleCompactAfterMinutes = 0`。上游兼容旧名 `sessionTtlMinutes`，建议新配置只写规范名。不要把 `idleCompactCheckIntervalSeconds` 设为 0 当作关闭，它控制扫描频率。当前 lover 的空闲阈值默认已为 0，上游默认是 15。

核对后的 lover 实际行为：

- token 压力达到有效输入预算时，分批压缩旧前缀，目标默认是预算的 50%（`consolidationRatio = 0.5`），保留近期原始后缀；受合法消息边界、压缩轮数与失败条件约束，不保证精确落在 50%。
- **并非只有 token 满才压缩**：回放消息窗口超过 `maxMessages` 也会先归档，当前 schema 默认 120。手动强制压缩、文件上限也是其他边界。迁移不能把这些旧条件不加选择地搬回。
- 物理裁剪减少活跃文件大小，但不会在“已从模型重放排除”的基础上再次减少模型 token；这是存储组织决定，与触发时机是两件事。

建议待实施方案落实为：到有效输入预算才自动触发（预留输出和安全空间，不等服务端报溢出）；优先压缩旧前缀、保留近期原文；可靠归档后再物理裁剪。50% 可作为沿用 lover 的初始候选，不因本次讨论新增复杂配置。

移植时保留上游累计摘要、工具调用边界和错误恢复；裁剪后必须同步摘要边界与 provider continuation state。归档失败不能继续裁剪，历史查询/分页需能读到移出的原文。支持 Responses 原生压缩的路径可能按约 90% 窗口提前触发，关闭空闲压缩不会关闭它，需要在集成方案中统一说明，不能承诺仅靠一个配置实现所有“满才压缩”的行为。

## 6. 桌面感知、语音、伴侣与技能

### 桌面感知：保留能力，触发方案另议

心跳用上游原生实现。桌面感知保留需求，但暂不决定沿用失焦截图、周期截图或每次心跳主动取屏幕等旧流程。

后续只需讨论三个实际问题：什么时机观察；锁屏/静默时段是否完全停止；观察到变化后什么情况值得主动说话。截图权限、图片保存位置与有效期也需要在具体方案中说明，不在迁移时默认打开旧行为。

### 语音与数字伴侣：已收敛

- 只迁移 MiniMax TTS 及所需配置/音色选择，保留自动朗读、手动重播、暂停系统媒体。
- 暂不迁移 IndexTTS、Qwen、其他语音后端和主备服务链路。
- 只迁移本地状态视频：待机、工作、说话等状态展示及需要的本地场景资源。
- 不迁移 LiveTalking/WebRTC 实时口型、THA、PSB/E-mote 服务与专属协议；离线素材制作管线不是运行必需项，暂不加入首轮。
- MiniMax 或视频资源不可用时，文字对话应继续正常。

### 日语教学：workspace 技能

现有代码已在 `deploy/skills/japanese-tutor/`，是独立 bundle，不必改成 `nanobot/skills/` 内置技能。新版本按工作区技能部署即可，保留其引用文件、脚本、课程与学习状态。Anki 和教材等外部数据另行保护，不塞进核心包。

## 7. 修订后的迁移顺序

| 阶段 | 内容 | 验收重点 |
| --- | --- | --- |
| A 固定底座 | 当前项目切换 lover-next，固定上游提交，隔离运行配置与数据副本 | 旧 lover 与日常运行不被新版本改写 |
| B 共用 UI 与桌面壳 | 最新 WebUI + Electron 宿主 + 统一收件箱 | 发送/历史/跨渠道路由、托盘、截图、通知与重连 |
| C 试用上游行为 | 对话、子代理、cron、heartbeat、压缩 | 先记录实际缺口，不预先搬旧定制；特别观察统一界面的任务噪声 |
| D 个人能力 | 外观、文件/笔记库、Active Memory、Dream 语义、MiniMax、本地视频 | 每项端到端可用；不带弃用后端 |
| E 桌面感知专项 | 讨论触发与通知方案后再接入 | 避免无意义观察与主动打扰 |
| F 原文保护与切换 | 旧 JSONL 对照、重置/删除归档边界、附件、配置与部署 | 原文可查；归档失败不继续删除；可退回旧环境 |

数据盘点和只读验证从 A 阶段开始；F 的持久性门禁必须在真实数据切换前完成。未决定的 F07 入口和归档组织形式不应阻塞 UI 底座试用。

试用上游自动化不意味着认可它一定适合统一收件箱；它与“主聊天少细节”的愿望之间的差异先观察，再以实际体验决定是否补过滤或执行隔离。

现有未跟踪的 `Pipfile`、天气规格目录保持不动。已按用户要求使用无前缀分支名 `lover-next`，当前目录为 `/Users/illusion/dev/self/nanobot`；不使用额外 worktree。分支起点是 `v0.3.5` 对应的 `1bb712d3488915ca4ed9ccc1a93067ff722f5ab9`。本地 main 为最新上游 `4420ad58`，与新分支的基线不同；标签之后的 18 个提交未合入新分支。本文对最新上游的分析需在实施时对照 v0.3.5 核验，不能假设标签包含所有后续修复。本文保存在当前项目。旧 lover 的源码引用需切回 lover 或使用 git show lover:<path> 查看；新分支尚未迁移的文件链接可能暂时不存在。

## 8. 源码依据与未决事项

本地：

- [月度原文归档实现](../nanobot/session/history_store.py)、[归档 grep 搜索](../nanobot/agent/tools/session_search.py)、[压缩与调用路径](../nanobot/agent/memory.py)
- [Active Memory](../nanobot/agent/active_memory.py)、[共用日记搜索函数与工具](../nanobot/agent/tools/diary_search.py)
- [Electron](../electron/)、[当前统一收件箱转发](../nanobot/channels/websocket.py)、[当前运行入口](../nanobot/cli/commands.py)
- [MiniMax 等 TTS 实现](../nanobot/providers/tts.py)、[workspace 日语教学 bundle](../deploy/skills/japanese-tutor/)

上游固定版本：

- [WebUI 原生宿主接口](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/webui/src/lib/runtime.ts)
- [会话摘要边界与 JSONL 保存](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/session/manager.py)
- [记忆与空闲压缩](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/agent/memory.py)、[空闲扫描](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/agent/autocompact.py)
- [上下文预算与压缩](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/agent/context_governance.py)
- [/new 与 /compact](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/command/builtin.py)、[默认配置](https://github.com/HKUDS/nanobot/blob/4420ad58e10c5a86f055eb498a7e4f0a59eaed6c/nanobot/config/schema.py)

剩余决策集中在：桌面感知的新触发方案；是否去掉 F07 专用工具入口；原文是否必须采用额外月度归档格式，以及清理/删除的具体交互。其余已按用户选择收敛，不重复讨论是否保留 THA、PSB、实时口型、其他 TTS 或天气。

## 9. 第一批实现：Electron 基础壳

已新增 `electron/`，由 Electron 主进程、受限 preload、连接页和 gateway 转发组成。根 `webui/` 源码未修改，直接使用其构建脚本生成桌面包内的 renderer。没有复制 React 组件或改动 Agent/Session 内核。

已提供：本机/NAS 地址选择和记忆、每 gateway 独立认证分区、上游原生 WebSocket 桥接、重新连接菜单、单实例、macOS 窗口生命周期、构建和当前平台打包入口。桌面试用偏好与旧 lover 隔离。

本批不包含：统一收件箱、托盘/全局快捷键/截图迁移、MiniMax、伴侣视频与记忆变更。界面仍展示上游多会话布局，后续按第 3 节的已确认方案接入统一聊天。

启动与打包见 [Electron README](../electron/README.md)。验证包括 WebUI TypeScript/Vite 构建、5 项脚本、附件边界与代理单元测试、真实 Electron 进程的模拟 gateway 消息收发与安全边界，以及 macOS arm64 应用打包。测试使用临时配置与模拟后端，不调用真实模型或改写 NAS 数据。

第一轮 review 修复：正式打包脚本的参数引用、签名图片附件的隔离预览、主聊天页麦克风权限与 macOS 用途说明。模拟设备验证音频可用、视频与附件页音频被拒绝；不替代真实麦克风和系统授权验收。

## 10. 统一收件箱第 1 步：固定身份与模型状态映射

本步已实现，未包含统一历史聚合、跨渠道实时转发或侧栏精简。前提是后端已开启 `agents.defaults.unifiedSession`；不在实现中改写用户配置。传给状态投影的 lambda 仅延迟读取这个配置值：开启时查询 `unified:default`，关闭时仍查询 `websocket:desktop`。Agent 的统一执行已有上游支持，新增的是 WebUI 查询目标对齐。

### 三个名字分别负责什么

| 标识 | 职责 | 本步行为 |
| --- | --- | --- |
| `desktop` | WebSocket 的 `chat_id`，决定向哪个桌面订阅发送事件 | Electron 固定使用，断线重连仍订阅此 ID |
| `websocket:desktop` | WebUI 的公开会话 key，用于页面路由和展示记录 | 保持上游格式；未发送消息前由客户端提供固定入口，不为此写空会话文件 |
| `unified:default` | Agent 实际运行的 session key，拥有模型历史、任务与摘要状态 | 发送和 `/stop` 使用上游既有统一路由；新增查询映射读取同一状态 |

发送链路：Electron preload 的 `fixedChatId` → WebUI RuntimeHost → NanobotClient → WebSocket 入站 → AgentLoop 现有统一路由。核心循环没有为伴侣模式增加另一套处理分支。

查询链路：`GET /api/sessions/websocket%3Adesktop/context` → WebUI 的 `model_session_key()` → 统一 Session 快照。重连的 attach/hydration 同样读取统一会话的模型预设、用量、goal 与恢复元数据；返回的实时事件仍标记 `chat_id=desktop`，让桌面能正确接收。

只将保留的 `websocket:desktop` 入口做此查询映射，普通浏览器的话题 ID 和查询语义不变。历史接口暂未改成统一历史；模型能续上对话与界面能展示全部旧历史是两项独立工作。

新建动作在固定模式复用已有入口，不发送 `new_chat`，也不等同 `/new` 或清空模型历史。侧栏暂时保留上游布局；点击其他话题进入聊天时会回到固定入口，临时/分叉创建被拒绝，后续再收敛这些 UI 入口。

验证：前端相关测试 200 项通过；后端身份、状态投影与统一执行测试 29 项通过，上下文 HTTP 路由两种配置分支已通过；Ruff、选定前端文件 ESLint、TypeScript/Vite 构建和正式打包通过。打包版模拟 gateway 冒烟确认发送及刷新均使用 `desktop`，不发 `new_chat`。未自动重启用户正在运行的 gateway 或 Electron，也未对真实模型发起测试请求。

## 11. 统一收件箱第 2 步：历史读取

已接入现有 `GET /api/sessions/websocket%3Adesktop/webui-thread`，没有新增 inbox API。开启统一会话时，以 `unified:default` 的持久化原文为唯一历史源；关闭时继续使用上游 WebUI transcript。原始文件只读，不复制、不清理，也不混入 `websocket:desktop` 或旧 `inbox:unified` 的流式文件。

读取过程：统一 Session JSONL → 公开内容转换 → 按整轮选择历史页 → 上游 `replay_transcript_to_ui_messages()` → 原有聊天组件。复用响应格式、分页游标编码和媒体签名处理；模型内部上下文、隐藏摘要检查点、私有 provider 状态不进入 UI。可见文本、附件、思考和已有工具调用/结果按现有组件格式显示，工具内联二进制沿用上游脱敏规则。

这里读取完整 Session 原文，而非 `get_history()` 的模型重放窗口，因此能显示压缩前仍保留的对话。旧 lover 已移入月度 archive 的内容还不在这条读取路径中，归档导入属于后续数据迁移。独立任务会话也不会在此步自动合并。

分页保持一轮对话完整，追加消息或插入隐藏摘要边界不会改变已有可见消息的回放 ID。统一数据源不使用 WebUI 流式文件的 ETag，避免源数据变化而缓存仍命中的问题。当前底层仍需读取统一会话文件，再选择展示页；本步没有新增索引或缓存系统。

尚未实现其他渠道实时事件转发；其他渠道的对话在统一会话保存后，可通过刷新加载。本步也不改侧栏布局、记忆存储或 cron/heartbeat 的执行策略。

验证：293 项后端回放/渠道测试通过，包含统一与非统一模式、压缩前原文、跨页稳定 ID、旧流式记录不混入、媒体、思考、工具结果和鉴权；Ruff 与改动生产文件的 BasedPyright 通过。网络测试仅在测试进程设置 localhost 直连以绕过本机代理。打包版 Electron 使用模拟 gateway 验证历史加载及发送后刷新不重复，未调用真实模型或修改用户数据。

### 历史 review 第 1 项修复

统一历史正文仍取 Session；额外从 WebUI 活动 transcript 只读最近的真实 `turn_end` ID 和未完成状态，填入 `completed_turn_ids`。客户端沿用原有完成标记机制拦截迟到的同轮分片，不按文本猜测轮次，也不生成伪造 ID。完成状态先读取、Session 快照后读取；落盘的完成标记优先于尚未清理的运行状态。轮转后最新一轮仍在活动文件中，不扫描整个流式归档。

新增验证覆盖完成记录轮转、未完成轮次不误报完成，以及客户端收到历史完成标记后拒绝晚到的 delta/turn_end。

另两项尚未改动：生成中刷新导致未落盘回复前缀缺失；超长单轮触发展示页容量限制时没有截断提示和完整内容入口。置灰刷新按钮只能减少误操作，不能替代断线/窗口重开的恢复；后者的裁剪发生在响应投影中，不会删除磁盘原文，也不是模型上下文压缩。

## 12. 本机历史文件名迁移与桌面单入口

本机验证发现，旧统一会话仍在 `unified_default.jsonl`，有 82 条原始消息；v0.3.5 只读取 Base64 编码文件名 `dW5pZmllZDpkZWZhdWx0.jsonl`，当时目标不存在，导致接口返回空历史。这不是内容格式不兼容，也不是原文被清理。

经用户确认，已停止本机 gateway，在私有备份目录保存原文件与校验清单，原样复制到新文件名后核对字节、消息数量，并重启 gateway。原文件保留，不增加永久旧文件名兼容分支。真实 HTTP 历史接口验证返回 40 条展示消息且支持继续加载更早历史。备份保存在本机 `.nanobot/backups/unified-history-migration-20260918-154536/`，不纳入源码。

桌面侧栏不再渲染普通话题列表，改为一个“统一收件箱”入口；旧标题、handle 前缀、新建/搜索话题和话题归档入口不再显示。其他功能导航保持，普通浏览器仍走上游列表。此更改仅影响展示，不删除旧会话、不重写服务器侧栏偏好。

## 13. 跨渠道完整消息自动同步

前两步及单入口 UI 已提交为 `f6e42e38`。本批新增 `DesktopInboxCoordinator`，在 gateway 生命周期中订阅已有 `TurnRunStatusChanged` 和 `SessionTurnPersisted` 事件，不改 AgentLoop 或原渠道的发消息流程。

路径：外部渠道在 `unified:default` 中开始处理输入/保存回复 → 向 `websocket:desktop` 发布 `SessionUpdatedEvent(scope="thread")` → 现有 ThreadShell 自动请求统一历史 → 渲染同一份持久化消息。通知不携带重复正文，不会再追加一套历史；重复通知只会重读，同一消息不重复插入。

用户来源在实际入站消息转为 Session 消息时记录，包含中途插入的 follow-up；回复落盘后记录外部渠道助手消息来源。使用 `source_channel`、`source_chat_id` 保存，展示时只显示渠道名。旧消息不猜测来源，桌面本身无需外部渠道标签。其他网页客户端参与统一会话时也可同步，来源为 websocket。

仅处理真实路由到统一会话的外部对话。桌面自身、未开启统一会话的独立对话、内部 system/cli、heartbeat 和独立 cron 不触发同步。正常轮次运行结束不再重复发 idle 通知；取消/失败未产生落盘事件时，会通知桌面重读已保存的部分。

范围明确为完整消息自动同步，不额外开启各渠道的逐字流式输出，也不更改它们的原有投递。用户输入在其开始执行并已落盘后显示；排队期间不伪造未持久化历史。

验证：89 项后端测试、134 项前端测试、Ruff、改动生产文件 BasedPyright、前端 ESLint、正式构建打包通过。打包版模拟 gateway 验证不刷新窗口即可显示外部消息和来源，重复通知不产生重复消息；未向真实聊天渠道发送测试内容。本批已完成验证。

### 跨渠道注入 review 修复

桌面正在生成时，外部消息可能被插入同一轮，不产生独立外部 running/persisted 事件。原实现只按外层事件渠道判断，漏掉了这些消息的来源和刷新通知。

已将用户来源记录放到正常入站落盘和 pending injection 转换的实际消息边界；注入消息通过既有私有 `_meta` 携带来源，保存时展开，provider 请求前由原有清理逻辑剔除。同步器跟踪所有统一会话轮次，检测本轮落盘的外部用户输入后通知桌面；纯桌面轮次仍不重复通知。不再扫描最后一条 user 来猜测其来源，也不改动调度、排队或模型调用行为。

新增回归先复现桌面轮次中的外部输入不触发通知，再验证修复；104 项相关后端测试通过，覆盖相同文本的多渠道来源、实际注入转换/保存及 provider 请求不携带来源字段。Ruff 和修改生产文件的 BasedPyright 通过。本次为后端修复，需重启 gateway 生效，未自动重启用户服务。

## 14. 单会话界面收尾

固定桌面模式不再启用上游分屏窗格，即使以前保存了包含 desktop 的多窗格布局，也只渲染当前统一会话。隐藏添加窗格、布局切换、消息分叉和临时聊天入口；禁用新话题/话题搜索快捷操作，保留其他功能导航。统一页面标题使用“统一收件箱”。普通浏览器行为不变，不删除旧话题或主动清理其布局数据。

同时修正消息组件的能力传递：没有分叉回调时不再传递空回调并显示无效按钮。

验证：125 项前端布局/侧栏/消息测试、ESLint、TypeScript/Vite 构建和正式打包通过；打包版冒烟覆盖分屏/分叉入口隐藏、历史读取、跨渠道自动同步与重复通知不重复显示。本批已完成验证。

## 15. F03 桌面基础体验

已接入托盘、关闭隐藏、全局显示/隐藏快捷键、截图附件、后台完成通知和开机启动菜单。沿用原 WebUI 的附件上传与预览流程，没有新增桌面专用聊天组件。

全局 `Cmd/Ctrl+Shift+E` 显示/隐藏窗口，快捷键冲突时保留托盘入口；应用内 `Cmd/Ctrl+Shift+S` 或桌面菜单截图。截图暂时隐藏窗口，只取鼠标所在屏幕，返回统一收件箱后作为普通图片附件等待手动发送。主进程负责截图，renderer 只接收结果；从其他页面返回时缓存图片直到输入框挂载。

主进程监听已有 WebSocket 完成事件；外部渠道落盘的 `session_updated` 增加可选 `notification_id`，开始同步不带此字段。桌面轮次使用 `turn_end`，包含外部插入时不额外发完成提醒。同轮去重，聚焦时不弹通知，通知正文不包含聊天内容。后端不增加另一套通知服务。

开机启动仅在 macOS/Windows 打包版中提供，读取系统当前状态，仅用户点击时修改；开发模式不注册登录项。托盘图片复用 lover 资源。全局快捷键可通过 `NANOBOT_RAISE_SHORTCUT` 覆盖或置空禁用，`NANOBOT_DESKTOP_NOTIFICATIONS=0` 可关闭系统通知。

验证：10 项 Electron 测试、20 项前端测试、221 项后端测试通过；Ruff、修改生产文件 BasedPyright、前端 ESLint、TypeScript/Vite 构建、正式打包与打包版模拟 gateway 冒烟通过。截图测试使用模拟图片，未读取真实屏幕；未更改系统登录项，真实截图授权、通知展示和开机启动仍待用户体验。需完全退出并重启 Electron，外部渠道完成通知需重启 gateway；未自动重启用户服务。本批作为独立 F03 提交。

### Review 修复与提交归组

修复输入框禁用期间截图被消费后丢失的问题：禁用时解除截图订阅，保留宿主待处理图片，恢复输入后再附加。回归测试覆盖禁用、收到截图、恢复输入及只附加一次。

F02 的原 `f6e42e38` 与 `ffc51182` 合并为 `0f40585f`（`feat(inbox): complete F02 unified desktop inbox`），合并后的文件树与原两笔提交完成时一致；历史章节中的旧哈希仅记录当时阶段。F03 独立提交。后续 review 修复继续时，完成且验证通过的 F 项直接提交，无需用户再次提醒；不自动推送远端。

## 16. F04 聊天展示与设置：确认复用上游

本项按已确认决定收尾：继续使用共享 WebUI 的现有实现，暂不移植 lover 的旧聊天展示和设置界面。F04 的“完成”表示现有能力已接入并通过针对性验证，不代表用户已完成实际体验或要求的新外观已经实现。

| 能力 | 当前实现与边界 |
| --- | --- |
| 思考与工具过程 | 使用 MessageBubble、ReasoningRow 及 thread/activity 中的工具展示；统一历史沿用已有回放转换 |
| 文件 diff | 使用现有文件编辑活动与 DiffSyntaxHighlight；展示依赖上游提供相应编辑事件，不补造旧记录缺失的数据 |
| 用量 | 使用现有 TokenUsageCard、ComposerUsagePopover 与 ThreadShell 用量计算，不另建桌面统计逻辑 |
| 设置 | App 继续加载共享 SettingsView；模型、提供方、运行配置与导航均沿用上游 |

验证：15 个相关前端测试文件共 217 项通过，覆盖思考展开、工具模型、消息展示、diff 高亮、用量卡片以及设置读取/修改流程；统一历史投影的 5 项后端测试通过，包含附件、思考和工具结果回放。设置测试使用模拟请求，未修改本机配置或调用真实模型。测试有既有 KaTeX quirks-mode 提示，未导致失败。

本项仅更新迁移进度和验收记录，不新增产品代码。后续按实际体验补具体缺漏；F05 负责主题、头像名称、透明与壁纸，F06 负责工作区和笔记浏览。

## 17. F05 第 1 步：桌面主题

已接入桌面九主题选择：九套配色以 lover 为起点，按当前 WebUI 的语义变量和对比度重新适配（详见下方审计）。不复制旧布局、气泡或全局样式；设置沿用上游外观页。普通浏览器仍显示原来的明暗切换。

主题名称保存在现有 localStorage 键 `nanobot-webui.theme` 中，随当前 gateway 的 Electron 分区隔离；不读取旧 lover 的偏好文件。ThemeProvider 仍向代码和 diff 高亮提供 light/dark 模式，midnight/neon 正确归入暗色；页头明暗切换会回到标准 light/dark。

验证：119 项主题、桌面外观、布局、设置及 diff 测试通过；ESLint、TypeScript/Vite 构建、正式打包通过。打包版模拟 gateway 冒烟覆盖九个主题入口、选中 midnight、返回聊天和重载恢复，并检查深夜主题聊天截图。首次设置页冒烟暴露模拟响应缺少设置域，已补齐测试夹具后通过，未修改真实 gateway 配置。

F05 尚未完成：头像名称、网络/本地目录壁纸及壁纸上的透明面板仍待接入。本步保持未提交，待该 F 项完成、review 修复及验证后归为一笔提交。

### F05 色板与 i18n 审计修复

此前只复制旧色板，漏了新 UI 的变量与组件消费方式。本轮按实际调用核对 Tailwind、聊天/侧栏、设置面板和控件，不把旧 CSS 整段搬回。

| 分类 | 处理 |
| --- | --- |
| 新 UI 表面色 | 增加 sidebar-content、sidebar-muted-foreground、sidebar-selected、settings-canvas、settings-surface 的映射；滚动条沿用当前语义变量 |
| 共用前景与边框 | card/popover/secondary 前景、input/ring、sidebar 前景/边框统一派生，移除逐主题重复定义 |
| 用户消息与操作 | 桌面气泡使用成对 primary/primary-foreground；开关、设置强调文字及主题选中态跟随主色 |
| 可读性 | 保留主题色相，调整主色/次要文字明度与前景搭配；回归检查主要文字背景对至少 4.5:1，不宣称含透明度和禁用态的所有最终像素均达此值 |
| 不属于色板 | 移除旧 radius 覆盖；不迁移旧气泡阴影、发光和纹理；品牌标识、语法高亮、错误/成功与用量图例保留语义区分 |
| 本地化 | 十种语言补齐七个主题名称；补齐其余七种语言缺失的统一收件箱名称；实际切换中文验证 |
| 首屏 | 预启动脚本识别九主题，桌面首屏即应用对应色板与明暗模式，避免等 React 挂载才恢复 |

检查包含九主题聊天/外观页截图与用户气泡实际计算颜色，亮色/暗色代表下的设置总览、模型、运行页，以及中文主题切换和重载。更完整设置页冒烟需要的 usage/api-service 模拟响应已补齐，不连接真实模型或修改个人配置。F05 的头像名称、壁纸与透明面板仍未完成，因此本批继续保持未提交。

本轮验证完成：217 项相关前端回归通过，随后新增的 9 项首屏主题恢复检查通过；最终长名称换行的 3 项外观回归通过。ESLint、正式构建打包与最终打包版冒烟通过，覆盖九主题用户气泡计算样式、主题名称不溢出、代表性设置页、中文名称和重载恢复。截图仅来自隔离的模拟 gateway。

## 18. F05 完成：头像名称、壁纸与透明面板

F05 已完成；第 17 节的未完成状态是主题阶段记录，以本节为准。桌面「设置 → 外观」现在提供九主题、本机显示名称、表情/简称、固定 gateway 头像，以及网络图片或本地目录壁纸。身份只用于侧栏头像与聊天展示，不修改模型人格或 gateway 配置。

名称、表情、壁纸来源/顺序/间隔和面板不透明度保存在 Electron 用户数据目录的 `appearance.json`（默认 `Nanobot-next/`），通过临时文件与 rename 保存。头像沿用 lover 的固定来源：gateway 媒体目录中的 avatar.jpg、avatar.png、avatar.webp（按此优先级），缺失或加载失败时显示表情或名称首字；Electron 不提供头像导入和存储。主题仍使用每个 gateway 分区的现有 localStorage。旧 lover 的 Electron 偏好不会自动导入；固定头像文件直接复用。

壁纸支持 HTTP(S) 图片、系统选择器授权的本地目录；目录按文件名数字顺序或随机轮播，尽量避免连续重复，并跳过损坏、移除的图片及目录符号链接。应用重启后从目录首张开始；轮播当前位置不另写偏好。网络请求不携带 gateway 凭据，输入图片上限 12 MiB，解码后以最长边 2560 像素的 JPEG 传给 renderer。目录扫描 PNG/JPEG/WebP/GIF/BMP，由系统解码器处理；不把 SVG/HTML 作为界面内容加载。

窗口隐藏时不发起壁纸刷新，恢复显示时刷新。失败时保留上一张并在外观页提示；可重试、换来源或关闭。面板不透明度可调 50%–100%，默认 80%；只在启用壁纸时调整布局、用户消息气泡和输入框的背景透明度，文字、按钮和弹层保持不透明。壁纸关闭时恢复普通布局，不是对整个窗口（连同文字）统一降低透明度。

实现复用现有 RuntimeHost 增加受限 appearance 接口，所有 IPC 校验当前主窗口与 frame；新壁纸目录必须由系统选择器授权。外观 Provider 向设置、侧栏和消息组件提供同一份状态，普通浏览器不提供桌面偏好入口。新增文案覆盖现有十种语言。

Review 已核对本机文件授权、网络响应大小与类型、原子保存、过期异步结果、隐藏刷新、失败保留图片、关闭后清理样式以及浏览器隔离。验证：238 项相关前端测试、14 项 Electron 测试通过；ESLint、TypeScript/Vite、正式打包通过。打包版隔离 gateway 冒烟覆盖设置中保存名称、重载后的身份展示、网络壁纸恢复、透明层计算样式、关闭壁纸，以及此前主题/历史/跨渠道同步回归。截图使用生成的测试图案；未读取真实屏幕或个人图片，未操作用户登录项或后端配置。系统文件选择器的选择/取消逻辑使用模拟对话框测试，未自动操作真实文件选择窗口。

后续验证约定：小修只运行直接相关的测试；一个 F 项收尾时做一次必要的集成验证。通过后不重复全套测试或打包冒烟，除非新增改动影响了对应链路或出现新失败。

### F05 提交 review 修复

将 DesktopAppearanceProvider 从入口外层移到 App 的 ready 分支，认证、加载和连接错误状态不挂载壁纸；退出 ready 状态时清理壁纸样式和刷新任务。壁纸关闭时，隐藏的无效间隔/透明度恢复默认；非网络来源的无效网址清空，合法设置仍保留，当前来源的字段继续严格校验。

仅运行针对性验证：5 项原生外观存储测试、2 项 App 壁纸生命周期回归，以及隔离 Electron 的认证页复现场景（`npm --prefix electron run test:smoke -- --auth`）。认证页截图确认标题与说明正常显示，不加载壁纸。ESLint 与前端构建通过；未重复运行完整测试、完整冒烟或重新打包应用。本轮修复合并进 F05 提交。

### F05 固定头像调整

按用户决定移除 Electron 头像选择、移除、序列化和 PNG 转换分支。恢复 lover 的固定 `/api/avatar`：只读取媒体根目录下的三个约定文件名，不接受任意路径，拒绝越出媒体目录的符号链接；作为固定公开头像提供，与 lover 行为一致。名称和表情偏好不变。旧 appearance.json 的 avatar 字段读取时忽略，后续保存时自然剔除，不主动修改用户文件。

本机已确认存在 `~/.nanobot/media/avatar.png`，无需复制或重新导入。只运行 5 项 Electron 外观、2 项后端固定头像、31 项前端身份与语言包检查；Ruff、相关 ESLint、前端构建通过。没有重跑完整冒烟或重新打包。后端新增固定头像路由，需重启 gateway；Electron 重启后加载新的宿主模块与界面。

### F05 壁纸表层补齐

按用户反馈，用户消息与底部输入框仅背景使用 `--desktop-panel-opacity`，不设置元素整体 opacity；输入框聚焦时继续保持该背景透明度。移除 composer dock 的实色底座，关闭壁纸后恢复原主题背景。前端构建与 `test:smoke -- --wallpaper-surfaces` 定向验证通过，覆盖明暗主题、聚焦/失焦、文字/元素 opacity 为 1 和关闭后恢复；已检查截图，未跑整套回归。
