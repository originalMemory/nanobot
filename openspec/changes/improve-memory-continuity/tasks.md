## 1. 基线与回放

- [x] 1.1 保存私人输入快照与哈希，标明来源截止和未提供的运行配置；按用户要求仅离线交付，不覆盖 NAS。
- [x] 1.2 建立 20 个脱敏回归情景与对应私人真实样例，分别标注应召回历史、禁止关系、当前状态和自然回复标准。
- [x] 1.3 记录下载日志和旧实现回放基线、代码中的历史输入约束；标明线上实际请求范围未验证，不改会话上限。

## 2. 提词与正式日记来源

- [x] 2.1 清理附件/runtime/reference 输入，校验输出来源、去重和最多五词，过滤序号与文件名，保留已知数值实体。
- [x] 2.2 自动召回与手动日记搜索共用正式来源规则，排除同步冲突副本；长期资格按独立日期计数，补真实形态的脱敏回归。
- [x] 2.3 校准空概要和正文区域提取，排除天气/导航；相关正文优先，保持多关键词覆盖及近期/跨期结果，单片段上限 400 字。

## 3. 卡片生成与召回

- [x] 3.1 删除未知词全库共现触发父卡更新的路径；独立候选经过自身资格判断，每轮最多一个新卡任务。
- [x] 3.2 更新建卡关系契约和 schema：同义名与专属实体分开，保存关系证据定位；旧版关系未经复核不可继承。
- [x] 3.3 用相关正文与规范来源内容生成指纹；来源读取失败保留旧卡，完整扫描后的去重/纠正允许来源数减少。
- [x] 3.4 资格缓存绑定规则版本和证据；失败区别于语义拒绝，同一证据失败冷却一小时，保留单飞、后台与原子写入。
- [x] 3.5 为日志补时间、规则版本、关联 ID、过滤/生成/跳过原因、正式候选和去重数；不记录完整私人上下文。
- [x] 3.6 验证“助手/商店不触发菜品卡、角色能触发作品卡、独立作品不被旧菜品分流、天气修改不重建、来源失败不覆盖”等回归。

## 4. 提示词与上下文质量

- [x] 4.1 探测既有 1.7B；无法连接或没有质量证据时保持近期上下文实验不启用，不擅自换模型，准备离线实验输入。
- [x] 4.2 调整 Consolidator：保留短期但重要的后续、事件日期与主体；避免工具流水淹没用户关心的事项。
- [x] 4.3 调整 Dream：区分事实纠正和时间变化，当前状态带日期，用户亲述与助手建议分离；不新增全卡扫描或常驻摘要。
- [x] 4.4 为归档/Dream 补日期、状态转折、主体和短期事项的确定性契约检查；模型遵循效果归入 5.3 在线验证。

## 5. 私人记忆整理

- [x] 5.1 生成四文件的私人迁移草稿：合并表达重复、保留人格与经历、标记时效、核实机制描述；逐项附来源和 diff。
- [x] 5.2 生成三张现有卡片与 decisions 的迁移草稿：保留阶段，清理错误继承，去重来源，处理不确定归属和旧版缓存。
- [ ] 5.3 完成私人真实样例 A/B，关键关系与事实零编造；模型样例三次运行，记录 20 个情景中至少 16 个同时准确自然的结果及前台延迟。
- [x] 5.4 按用户要求输出独立下载子目录中的迁移包，核对输入不变和输出哈希，提供手动差异合并与回滚说明；不更新 NAS。

## 6. 集成验收与交付

- [x] 6.1 运行相关 grep/memory/context 测试与目标文件 ruff check；重放已成功的跨日聊天正例，确认没有强行翻旧事。
- [x] 6.2 验证旧卡降级、失败冷却、来源缩减与原子写入失败；离线包保留基线和 diff，说明 NAS 新写入的合并/回滚规则；确认私人原文未进入仓库。
- [x] 6.3 汇总代码/提示词/私人记忆实际 diff、质量对照、延迟和未解决项；仅部署已获授权部分，Git 提交单独遵循用户指令。

## 验证证据与待办边界

- 本地代码/提示词与离线七文件交付完成；相关测试 218 项通过，ruff、diff 检查、OpenSpec 严格验证通过。
- 20 个真实检索情景通过；同一 1.7B 每情景三次实测，最终单消息方案原集 17/20、附加挑战 3/3；近期上下文 12/20 且有错误跨话题，明确不启用。
- 5.3 中主聊天模型最终回复 A/B 未做：该模型配置/请求未提供，不能以提词和检索结果代替。用户更新 NAS 后再做，未标完成、未归档。
- NAS 部署按用户要求交由用户手动执行，私人包包含原件备份、hash、diff、使用与回滚说明。


## 7. 用户确认的卡片字段精简

- [x] 7.1 实体引用仅在生成时校验，保存简短来源定位；保留主题、别名与专属实体匹配。
- [x] 7.2 从 sources 派生时间范围，删除重复计数、废弃拒绝名单及迁移说明；保留平级关系。
- [x] 7.3 更新下载目录三张卡、hash/diff 和说明，验证紧凑格式的生成、召回、日期展示及真实回放。

## 8. 删除无用平级关系并评估门槛

- [x] 8.1 删除related_topics的生成与保存，清理交付文件并更新差异/哈希，相关测试通过。
- [x] 8.2 对照真实日记评估现行20日期/90天门槛，输出5日期/不限跨度建议；未更改常量或接入索引归属判断。


## 9. 放宽送审与统一归属判断

- [x] 9.1 五个独立日期即可送审，取消九十天门槛，保留每轮一个候选及完整证据检查。
- [x] 9.2 将已有卡片精简索引传入模型，接入alias/entity/new/no_save四种处理，引用校验后补父卡映射。
- [x] 9.3 no_save统一“本次不保存”，缓存随证据/索引/规则变化失效；执行错误独立冷却。
- [x] 9.4 验证四种结果、缓存失效、并发更新与门槛边界，更新下载包说明及验证记录。


## 10. Review 回归修复

- [x] 10.1 重建保留已确认映射，只允许有证据的显式撤销，临时撤销声明不落盘。
- [x] 10.2 旧父卡完整升级后重新审核并补映射；失败不伪报成功。
- [x] 10.3 按文件隔离读取失败，保留健康片段并禁止不完整来源写卡。
- [x] 10.4 手动搜索先过滤再截断，补充OR结果及零limit边界。
- [x] 10.5 跑四项回归、相关测试和真实回放，更新交付验证说明。
