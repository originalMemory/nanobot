---
description: 禁止在用户明确确认前自动执行 git commit
alwaysApply: true
---

# Git 提交需用户确认

**严禁**在用户明确要求前自动执行 `git commit`（包括 `--amend`）。

## 规则

- 完成代码改动后，**展示变更摘要**，然后询问「是否提交？」，等待用户回复后再执行。
- 用户说「提交」「commit」「提交一下」等明确指令时才可执行。
- 用户说「继续」「做下一个」时**不视为提交许可**。

## 例外

- `git add`、`git status`、`git diff`、`git log` 等只读或暂存操作无需确认。
