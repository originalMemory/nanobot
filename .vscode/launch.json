{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Electron: 启动",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm start",
      "cwd": "${workspaceFolder}/electron"
    }
  ]
}
